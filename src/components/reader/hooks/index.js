/**
 * Reader Hooks Index
 *
 * Reader-specific hooks for state management and functionality.
 * These hooks are designed to work within the reader context.
 */

export { useReaderState, ACTIONS as READER_ACTIONS } from './useReaderState';
