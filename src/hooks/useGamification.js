/**
 * useGamification — re-exports from GamificationContext.
 * All logic lives in GamificationProvider (single shared state).
 */
export { useGamification } from '../contexts/GamificationContext';
